/**
 * wordcount_maple0.cpp
 * Maple 0 phase for wordcount.
 */

#include <iostream>
#include <sstream>

using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    string input;
    while (cin >> input) {
        cout << input << "\t1" << endl;
    }
    return 0;
}
